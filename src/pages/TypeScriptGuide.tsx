function TypeScriptGuide() {
  return (
    <div className="example-card">
      <h2>TypeScript Guide</h2>
      <p>Use TypeScript to add type safety to your React and Redux code.</p>
      <ul>
        <li>Define interfaces for state and props.</li>
        <li>Use typed hooks like <code>useSelector</code> and <code>useDispatch</code>.</li>
        <li>Enable strict mode in <code>tsconfig.json</code> for better checks.</li>
      </ul>
    </div>
  )
}

export default TypeScriptGuide
